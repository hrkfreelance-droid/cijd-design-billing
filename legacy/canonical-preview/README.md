# Canonical preview source

This directory preserves the original `app/` implementation from canonical
commit `9547234` during the local integration. It is retained for provenance
and is not the active Next.js route tree; the integrated application runs from
`src/app`.

The canonical GitHub Pages-related configuration remains at the repository
root. No deployment or external hosting operation is performed by this branch.
